[package]
name = "mc-launcher-core"
version = "0.1.0"
edition = "2021"
description = "Core library for mc-launcher"
license = "MIT"

[dependencies]
reqwest = { version = "0.11", features = ["blocking", "json"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
directories = "6.0"
